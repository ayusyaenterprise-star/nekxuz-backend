// Add this console log right after app.use(cors(corsOptions))
console.log('[CORS] CORS middleware initialized with options:', corsOptions);
console.log('[CORS] Allowed origins:', corsOptions.origin);
